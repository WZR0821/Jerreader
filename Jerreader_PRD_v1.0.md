# Jerreader 产品需求文档（PRD）

> 文档版本：v1.0  
> 更新日期：2026-07-14  
> 目标平台：iPhone / iOS 18 及以上  
> 文档用途：供 Codex 进行产品规划、技术设计、分阶段实现和验收

## 1. 产品概述

### 1.1 产品名称

暂定名称：**Jerreader**

### 1.2 产品定位

Jerreader 是一款面向日语和英语学习者的 iOS 电子书阅读应用。用户可以导入自己拥有的无 DRM 电子书，在尽量保留原书排版的阅读界面中阅读，并通过点击单词或选择文本获得读音、释义、用法和中文翻译。

产品的核心原则是：**原文阅读优先，辅助信息按需出现，不用双语正文破坏原书排版。**

### 1.3 目标用户

- 正在学习日语或英语，需要阅读原版书的用户。
- 希望在阅读过程中快速查词、听发音、查看用法的用户。
- 不希望在电子书正文中插入大段译文、破坏原始版式的用户。

### 1.4 核心价值

- 在同一个阅读界面中完成阅读、查词和翻译。
- 点击单词即可查看学习信息，减少复制到其他应用查询的操作。
- 翻译采用弹窗呈现，保持原书字体、段落、图片、标题和章节结构。
- 支持日语和英语两种主要学习语言，并统一翻译为简体中文。

## 2. 产品目标与成功标准

### 2.1 V1 产品目标

1. 用户能够从 iOS“文件”中导入无 DRM 的 EPUB 电子书。
2. 用户能够在应用内正常阅读 EPUB，并保存阅读进度。
3. 用户点击日语或英语单词时，可以查看释义、读音和基础用法。
4. 用户选择句子或段落后，可以查看简体中文翻译。
5. 查词和翻译结果以弹窗或底部浮层显示，不修改 EPUB 正文和原始排版。
6. 已查询内容可以缓存，重复查询时快速显示。

### 2.2 V1 成功标准

- 至少能稳定导入并打开 5 本不同来源的标准无 DRM EPUB 2/3 测试书。
- 关闭并重新打开应用后，书籍和上次阅读位置仍然存在。
- 日语和英语横排 EPUB 可以正常翻页、滚动、调整字号和切换主题。
- 点击普通正文单词后，词语弹窗能在可接受时间内出现。
- 选择一段文字后，翻译结果不会替换、重排或永久修改正文。
- 应用在没有配置在线服务时仍可编译、启动和完成基础阅读流程。

## 3. V1 范围

### 3.1 本期必须实现（P0）

- EPUB 文件导入与本地书架。
- EPUB 阅读器。
- 阅读位置保存与恢复。
- 原排版阅读。
- 日语、英语文本识别。
- 点击单词弹出词语解释。
- 词语读音展示与系统语音播放。
- 选择句子或段落后翻译成简体中文。
- 翻译和释义弹窗。
- 查询缓存和基础历史记录。
- 加载、失败、无网络和不支持格式等错误状态。

### 3.2 建议实现（P1）

- 收藏单词和生词本。
- 查词历史、翻译历史。
- 复制原文或译文。
- 日语词形还原，例如将「食べました」识别为「食べる」。
- 英语词形还原，例如将 “went” 识别为 “go”。
- 深色模式、字号、行距、页边距和横向/纵向阅读设置。
- 目录导航和书内搜索。

### 3.3 本期明确不做

- PDF、扫描 PDF 和 OCR。
- MOBI、AZW、AZW3、Kindle 商店电子书。
- 任何 DRM 破解或受保护内容导入。
- 在正文段落之间插入译文。
- 自动批量翻译整本书。
- 用户注册、付费、订阅和云同步。
- 跨设备同步和多人协作。
- iPad、macOS 和 Android 的专门适配。
- 在 V1 中承诺日语音调核、复杂语源或专业词典级解释。

## 4. 产品假设与默认决策

为避免开发过程中反复等待确认，V1 使用以下默认决策：

| 项目 | V1 默认决策 |
| --- | --- |
| 源语言 | 日语、英语，可自动识别，也允许用户手动修正 |
| 目标语言 | 简体中文 |
| 电子书格式 | 无 DRM 的可重排 EPUB 2/3 |
| 最低系统版本 | iOS 18 |
| 阅读布局 | 优先遵循 EPUB 原有 CSS、段落、图片和章节结构 |
| 句段翻译 | Apple Translation Framework 优先 |
| 单词发音 | AVSpeechSynthesizer 播放；界面同时显示可获得的文本读音 |
| 单词解释 | 通过可替换的 LexicalLookupService 提供；开发阶段允许使用 Mock 数据 |
| 在线密钥 | 不得硬编码在 iOS 客户端；如需第三方 AI，必须通过后端代理 |
| 离线状态 | 阅读必须可用；已缓存结果可用；新查询根据服务能力降级 |

## 5. 核心用户流程

### 5.1 导入并阅读电子书

1. 用户进入书架。
2. 点击“导入电子书”。
3. 系统打开 iOS 文件选择器。
4. 用户选择一个 `.epub` 文件。
5. 应用校验文件是否可读取、是否为支持的 EPUB、是否存在 DRM 或解析错误。
6. 导入成功后，书架显示封面、书名和作者；缺少元数据时显示默认信息。
7. 用户点击书籍进入阅读器。
8. 阅读器恢复上次阅读位置；首次打开时从正文起始位置进入。

### 5.2 点击单词查词

1. 用户在正文中短按一个日语或英语单词。
2. 阅读器识别点击位置对应的词语和必要的上下文。
3. 页面保留原来的排版，并对当前词语做临时高亮。
4. 页面底部弹出词语卡片。
5. 卡片展示词语、读音、词性、中文解释和简短用法。
6. 用户可以播放读音、复制、收藏或展开查看更多内容。
7. 用户下滑关闭弹窗或点击弹窗外区域返回阅读。

若 EPUB 内核暂时无法可靠支持“短按命中单词”，开发阶段允许先实现“长按选择单词后点击查词”，但必须将短按查词保留为正式验收目标，不得静默删除需求。

### 5.3 选择句子或段落翻译

1. 用户长按并拖动选择一句或一段文字。
2. 系统选择菜单显示“翻译”。
3. 用户点击“翻译”。
4. 页面保持当前位置和原始版式，选中内容可临时高亮。
5. 底部弹出翻译卡片，显示原文和简体中文译文。
6. 用户可以复制译文、重新翻译或关闭弹窗。
7. 关闭弹窗后，正文恢复普通阅读状态，不插入任何译文。

### 5.4 恢复阅读

1. 用户退出阅读器或应用进入后台。
2. 应用保存当前 Readium Locator 和阅读设置。
3. 用户再次打开书籍。
4. 应用恢复到尽量接近上次离开的位置。

## 6. 功能需求

### 6.1 书架与导入

#### FR-BOOK-001 文件导入

- 使用 SwiftUI `fileImporter` 或等价的系统文档选择能力。
- V1 仅允许选择 `.epub`。
- 应用应将获得授权的文件复制到自己的持久化目录，避免临时访问权限失效。
- 导入时显示进度或明确的处理中状态。

#### FR-BOOK-002 EPUB 校验

- 检查文件是否可访问、能否被 Readium 解析。
- 对损坏文件、加密文件、重复文件和不支持格式给出可理解的错误提示。
- 不得尝试绕过 DRM。

#### FR-BOOK-003 元数据与书架

- 尽可能读取封面、书名、作者和语言。
- 缺少封面时显示默认封面。
- 书架至少支持打开和删除书籍。
- 删除书籍前需要确认；删除后同时清理其本地副本和关联阅读数据。

### 6.2 阅读器

#### FR-READER-001 EPUB 渲染

- 使用 Readium Swift Toolkit 的 EPUB Navigator。
- 阅读器允许嵌入 SwiftUI，但不得重新实现 EPUB 排版引擎。
- 支持章节导航、上一页/下一页或连续滚动中的至少一种稳定模式。

#### FR-READER-002 保留原排版

- 默认遵循 EPUB 自带的标题、段落、强调、图片、脚注和章节顺序。
- 查词和翻译不得把结果写入 EPUB HTML，不得永久改变 DOM 或 CSS。
- 临时高亮只能作为覆盖式装饰，关闭弹窗后可清除。
- 用户主动修改字号、主题或行距属于阅读偏好，不视为破坏原排版。

#### FR-READER-003 阅读进度

- 使用 Readium Locator 保存当前位置。
- 在位置变化、退出阅读器和应用进入后台时进行节流保存。
- 重新打开书籍时恢复位置；恢复失败时应回退到最近有效位置。

#### FR-READER-004 阅读设置

- P0：字号、浅色/深色主题。
- P1：行距、页边距、滚动/翻页模式。
- 设置按书籍保存，或提供明确的全局设置策略。

### 6.3 单词点击与词语卡片

#### FR-WORD-001 单词命中

- 点击普通正文单词时识别完整词语，而不是仅返回单个字符。
- 日语应处理假名、汉字和常见活用边界。
- 英语应处理标点、大小写、所有格和常见词形变化。
- 同时保留所在句子作为上下文，供用法解释使用。
- 链接、脚注、图片区域和翻页手势优先级需要明确定义，避免点击冲突。

#### FR-WORD-002 日语词语卡片

至少展示：

- 原始词形。
- 基本形；无法确定时允许省略并标记。
- 假名读音。
- 词性。
- 1 至 3 条简体中文核心释义。
- 一条结合当前句子的简短用法说明。
- 系统语音播放按钮。

#### FR-WORD-003 英语词语卡片

至少展示：

- 原始词形。
- 词元或基本形。
- 可获得时显示音标；无法可靠提供时不得伪造。
- 词性。
- 1 至 3 条简体中文核心释义。
- 一条结合当前句子的简短用法说明。
- 系统语音播放按钮。

#### FR-WORD-004 交互方式

- 使用底部弹出的 Sheet 或等价覆盖层，不跳转到新页面。
- 初始高度只展示核心内容，允许向上展开。
- 支持关闭、播放、复制；收藏为 P1。
- 弹窗不得导致阅读器重新分页或丢失位置。

### 6.4 句子和段落翻译

#### FR-TRANS-001 文本选择

- 用户可以选择连续文本。
- 选择菜单提供“翻译”操作。
- 需要过滤纯空白、极短无意义内容和超过上限的文本。
- V1 建议单次最多翻译 2,000 个 Unicode 字符；超出时提示用户缩短选择范围。

#### FR-TRANS-002 翻译结果

- 自动判断日语或英语，用户可手动修正源语言。
- 目标语言固定为简体中文。
- 翻译弹窗显示原文、译文、语言方向和错误状态。
- 支持复制译文和重新翻译。
- 不在正文中插入译文。

#### FR-TRANS-003 翻译服务

- 通过 `TranslationService` 协议隔离具体实现。
- 默认实现优先使用 iOS Translation Framework。
- UI 和数据层不得依赖某个第三方 API 的具体响应结构。
- 相同标准化原文与语言方向应优先命中缓存。

### 6.5 读音与语音

#### FR-AUDIO-001 语音播放

- 使用 `AVSpeechSynthesizer` 或系统等价能力。
- 日语词语使用日语语音，英语词语使用英语语音。
- 用户可再次点击停止或重新播放。
- 没有合适语音时显示明确提示，不应崩溃。

#### FR-AUDIO-002 文本读音

- 日语优先显示假名读音。
- 英语音标仅在数据源可靠提供时显示。
- 系统语音输出不能替代界面中的文本读音字段。

### 6.6 缓存与历史

#### FR-DATA-001 翻译缓存

- 缓存键至少包含：标准化原文、源语言、目标语言、服务版本。
- 缓存记录包含结果、创建时间和最后访问时间。
- 服务失败时，如果存在旧缓存，可以显示旧结果并注明来源。

#### FR-DATA-002 查词记录

- 保存词语、基本形、语言、所在书籍、简短上下文和查询时间。
- P0 允许仅用于缓存和调试，不必提供完整历史页面。
- P1 提供用户可见的查词历史和收藏管理。

## 7. 弹窗信息结构

### 7.1 词语弹窗

```text
食べました                    🔊
たべました｜基本形：食べる｜动词

吃；食用

当前语境：表示已经吃过或完成了进食动作。

[复制] [收藏]                 [展开]
```

### 7.2 翻译弹窗

```text
日语 → 简体中文

原文
彼は静かに本を閉じ、窓の外を見た。

翻译
他静静地合上书，望向窗外。

[复制译文] [重新翻译]         [关闭]
```

### 7.3 弹窗视觉要求

- 使用 iOS 原生 Sheet 交互习惯。
- 不遮挡全部正文，默认显示中等高度。
- 支持下滑关闭。
- 深色模式下保证对比度。
- VoiceOver 能读出原文、读音、释义和按钮名称。

## 8. 数据模型建议

数据模型可根据 Readium API 调整，但必须保持职责清晰。

### Book

- `id`
- `title`
- `author`
- `language`
- `localFileURL`
- `coverReference`
- `fileFingerprint`
- `importedAt`
- `lastOpenedAt`

### ReadingState

- `bookID`
- `locatorJSON`
- `fontScale`
- `theme`
- `readingMode`
- `updatedAt`

### TranslationCacheEntry

- `id`
- `normalizedSourceText`
- `sourceLanguage`
- `targetLanguage`
- `translatedText`
- `providerIdentifier`
- `providerVersion`
- `createdAt`
- `lastAccessedAt`

### WordLookupEntry

- `id`
- `bookID`
- `surfaceForm`
- `lemma`
- `reading`
- `language`
- `partOfSpeech`
- `definitions`
- `usageNote`
- `sentenceContext`
- `createdAt`
- `isFavorite`

## 9. 技术架构约束

### 9.1 推荐技术栈

- Swift 6 或当前 Xcode 稳定版默认 Swift 工具链。
- SwiftUI：书架、设置、弹窗和通用界面。
- Readium Swift Toolkit：EPUB 解析、导航、位置与文本交互。
- `UIViewControllerRepresentable` 或 Readium 官方推荐桥接方式：将 EPUB Navigator 嵌入 SwiftUI。
- SwiftData：书籍元数据、阅读状态、缓存和历史。
- Apple Translation Framework：句子和段落翻译。
- NaturalLanguage Framework：语言识别、分词或词元处理的辅助能力。
- AVFoundation / AVSpeechSynthesizer：语音播放。
- Swift Package Manager：第三方依赖管理。

### 9.2 服务协议

至少定义以下可替换协议，避免 UI 直接调用平台或网络服务：

```swift
protocol TranslationService {
    func translate(
        text: String,
        sourceLanguage: LanguageCode?,
        targetLanguage: LanguageCode
    ) async throws -> TranslationResult
}

protocol LexicalLookupService {
    func lookup(
        word: String,
        sentenceContext: String?,
        language: LanguageCode
    ) async throws -> WordExplanation
}

protocol SpeechService {
    func speak(_ text: String, language: LanguageCode) async throws
    func stop()
}
```

开发初期必须提供 Mock 实现，使应用不依赖真实 API 也能进行 UI、状态和测试开发。

### 9.3 安全约束

- 不得在仓库、源码、配置文件或编译产物中提交真实 API Key。
- 如果未来接入 OpenAI、DeepL 或其他在线 AI，客户端只能调用受控后端代理。
- 导入的电子书默认保存在应用沙盒内，不上传到第三方服务。
- 发送在线翻译时只发送用户主动选择的文本及必要上下文，不上传整本书。
- 在首次使用在线服务前，应向用户说明会发送哪些内容。

## 10. 非功能需求

### 10.1 性能

- 打开普通 EPUB 时必须显示明确加载状态，主线程不得长时间阻塞。
- 查词弹窗应先显示加载骨架或缓存内容，不得让点击看起来无响应。
- 翻译、查词、解析和数据库操作使用结构化并发，并支持取消。
- 快速连续点击不同单词时，旧请求不得覆盖最新词语的结果。

### 10.2 稳定性

- 无网络、服务超时、语言模型未下载、书籍损坏时不得崩溃。
- 阅读进度写入失败不能阻断阅读，应记录并在适当时机重试。
- 视图关闭后，异步翻译结果不得错误更新已销毁或已切换的弹窗。

### 10.3 可访问性

- 关键按钮提供 VoiceOver 标签。
- 支持系统动态字体；阅读器字体由阅读设置控制。
- 颜色不能成为唯一状态提示。
- 弹窗按钮点击区域符合 iOS 可用性要求。

### 10.4 隐私

- 默认不上传整本 EPUB。
- 本地阅读记录和查询记录默认仅保存在设备上。
- 日志中不得输出完整书籍内容、用户选择的长文本或任何密钥。

## 11. 错误状态与提示

必须覆盖以下状态：

- 文件不是 EPUB。
- EPUB 损坏或无法解析。
- EPUB 存在不受支持的 DRM。
- 文件已导入。
- 本地文件被删除或无法访问。
- 无法识别源语言。
- 翻译语言模型尚未下载。
- 翻译暂不可用或请求超时。
- 选中文字过长。
- 未找到可靠的单词解释或读音。
- 当前 EPUB 内容不支持准确的点击查词。

提示应告诉用户发生了什么以及下一步可以怎么做，避免只显示内部错误码。

## 12. 验收标准

### AC-01 导入

- 给定一个有效的无 DRM EPUB，用户可以从“文件”中选择并导入。
- 导入后书架显示该书；应用重启后仍存在。
- 导入损坏或受保护文件时显示错误，不崩溃。

### AC-02 阅读与排版

- 用户可以打开书籍并阅读正文、图片和章节。
- 查词和翻译操作不会把中文写入正文或改变 EPUB 文件。
- 关闭弹窗后仍停留在原阅读位置。

### AC-03 阅读进度

- 用户阅读到中间位置后退出，再次打开时恢复到相同或足够接近的位置。

### AC-04 日语查词

- 点击或选择一个常见日语词语后，弹窗显示读音、词性、中文释义和用法说明。
- 点击语音按钮能使用日语语音播放。

### AC-05 英语查词

- 点击或选择一个常见英语单词后，弹窗显示基本形、词性、中文释义和用法说明。
- 点击语音按钮能使用英语语音播放。

### AC-06 翻译

- 选择日语或英语句子并点击“翻译”后，弹窗显示简体中文译文。
- 翻译过程中有加载状态；失败时可重试。
- 重复查询相同内容时能够使用缓存。

### AC-07 构建与测试

- 项目在指定 Xcode 稳定版本下成功构建。
- 核心数据和服务协议至少有单元测试。
- Mock 服务下可完成导入后的主要 UI 流程，不要求真实在线密钥。

## 13. 开发里程碑

### Milestone 0：项目初始化

- 创建 iOS 18+ SwiftUI 项目。
- 配置 Swift Package Manager 和 Readium。
- 创建基础目录、服务协议和 Mock 服务。
- 建立最小测试目标。
- 确认模拟器构建通过。

### Milestone 1：导入与书架

- 文件选择器。
- EPUB 校验和复制。
- 书籍元数据解析。
- SwiftData 持久化。
- 书架展示、打开和删除。

### Milestone 2：EPUB 阅读器

- Readium Navigator 集成。
- SwiftUI 与 UIKit 桥接。
- 目录、基础主题和字号。
- 阅读位置保存与恢复。

### Milestone 3：文本选择与翻译

- 获取选中文本。
- 添加“翻译”操作。
- TranslationService、平台实现和缓存。
- 翻译 Sheet 与错误状态。

### Milestone 4：点击查词

- 单词点击命中或阶段性长按选择方案。
- 日语、英语分词和基本形处理。
- LexicalLookupService 与 Mock/真实实现。
- 词语 Sheet、读音和语音播放。

### Milestone 5：历史、优化与 QA

- 查询缓存与历史。
- 快速连续操作、取消和错误恢复。
- 多本 EPUB 兼容测试。
- VoiceOver、深色模式和性能检查。

## 14. Codex 工作规则

1. 开始实现前，先读取本 PRD、现有 `AGENTS.md` 和仓库结构。
2. 若仓库为空，先完成 Milestone 0；不要一次实现所有里程碑。
3. 每个里程碑开始前给出短计划，结束时报告修改文件和验证结果。
4. 每次只实施用户明确指定的里程碑，不擅自增加登录、云同步、支付、PDF 或整本翻译。
5. 依赖使用当前官方稳定版本；添加依赖前说明用途和替代方案。
6. 优先使用平台能力和 Readium，不自行重写 EPUB 渲染引擎。
7. 不得提交真实密钥、证书、个人 Team ID 或本机私有路径。
8. 任何真实在线服务都必须有 Mock 实现，测试不得依赖外网。
9. 修改完成后运行可用的构建、单元测试和静态检查。
10. 若当前环境无法运行 Xcode 构建，必须明确报告未验证内容，不得声称构建成功。
11. 短按单词查词若受 Readium 限制，应先做技术验证；临时降级方案必须被标注，不得当作需求完成。
12. 保持原排版是硬性要求：翻译和解释只能通过覆盖层、Sheet 或临时装饰展示。

## 15. 交给 Codex 的第一条执行提示词

将本文件放在仓库根目录后，可向 Codex 输入：

```text
请读取仓库根目录的 Jerreader_PRD_v1.0.md，并检查当前仓库与开发环境。

现在只处理 Milestone 0，不要实现书架、阅读器、翻译或查词功能。

请先：
1. 总结你对产品目标、V1 范围和硬性约束的理解。
2. 检查 Xcode、Swift、iOS SDK、Git 和当前项目状态。
3. 查询 Readium Swift Toolkit 当前稳定版本及 iOS 18/Swift 工具链兼容性。
4. 给出 Milestone 0 的文件结构、依赖和验证计划。
5. 指出真正阻碍初始化的问题；最多询问三个问题。

确认计划后再实施 Milestone 0：
- 创建或整理 iOS 18+ SwiftUI 工程。
- 通过 Swift Package Manager 配置必要依赖。
- 建立清晰但不过度设计的目录结构。
- 定义 TranslationService、LexicalLookupService 和 SpeechService 协议。
- 为翻译和查词提供 Mock 实现。
- 添加最小单元测试。
- 创建简短的 README 和适合本项目的 AGENTS.md。
- 运行构建与测试，并报告结果。

禁止事项：
- 不得硬编码 API Key。
- 不得在本阶段实现 PDF、DRM、在线后端或整本翻译。
- 不得声称没有实际运行的构建或测试已经通过。
```

## 16. 待后续确认的问题

以下问题不阻碍 Milestone 0 至 Milestone 2，可在实现真实查词服务前确认：

1. 单词解释优先采用离线词典数据，还是在线 AI 服务。
2. 是否需要生词本和导出功能进入 V1 正式范围。
3. 是否计划上架 App Store；这会影响隐私说明、后端和账号配置。
4. 是否需要支持日语竖排 EPUB。Readium 支持能力和实际书籍兼容性需要单独验证。
5. 英语读音是否必须展示 IPA；若必须，需要选择可靠且许可允许的数据源。
